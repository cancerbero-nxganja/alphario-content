# Analytics

Private credentials file (`credentials-private.json`) is gitignored — stored locally only.

## Public tracking
- `build-status.json` — system status
- `revenue-log.json` — daily revenue (public)
- `content-log.json` — published content tracker

